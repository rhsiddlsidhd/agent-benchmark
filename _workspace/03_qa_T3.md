## T3 — 공통 UI 컴포넌트 + QueryProvider — 판정: FIX

- 검증자: qa · 2026-07-05
- 빌드/타입/린트: `npx tsc --noEmit` PASS, `npx eslint src` PASS, `npm run build`(Next 16.2.10 Turbopack) PASS.

### API↔훅
N/A — T3는 UI 컴포넌트 + QueryProvider만 포함(라우트 핸들러·데이터 훅 없음).
- QueryProvider `defaultOptions.queries.retry: 1` — §4/ADR-0004 rate limit 정책과 일치. `useState(()=>new QueryClient)` 1회 생성, `refetchOnWindowFocus:false`, `staleTime:60_000` 정상. 커스텀 재시도 루프 없음(정책 준수).

### 에러/엣지케이스
- 이미지 결측(poster/backdrop/profile null) → `ImagePlaceholder`(아이콘+이니셜) 렌더, `role="img"`+`aria-label` 부여. PASS.
- 텍스트 결측: `ContentCard` 연도 없음 → "연도 미상", `PersonAvatar` 역할 없음 → 줄 생략, `RatingBadge` value<=0 → null. §2.9 준수.
- `ErrorState` role="alert" + onRetry(reset/refetch 위임), 없으면 버튼 숨김. `EmptyState` action 위임형(server 호환). 존재하지 않는 id는 not-found.tsx로 위임(주석 명시). PASS.
- 무한스크롤 종료 처리는 T3 범위 아님(소비 컴포넌트 T4+).

### 라우팅
- `src/app/`에 실제 존재하는 라우트: `/`, `/_not-found`(빌드 산출 확인).
- `layout.tsx` 헤더 `href="/search"`(24행대)는 아직 미존재 라우트를 가리킴 → 현재 404. 단, 이 nav는 T3 이전부터 존재하고 `/search`는 §3.2 계획 라우트(후속 태스크). **T3 결함 아님(forward reference, 비차단)**. 후속 검색 태스크에서 라우트 생성 시 해소 확인 필요.

### 디자인 토큰
- 임의 색상(hex)·arbitrary spacing(`[17px]` 등) 없음(grep 확인). PASS.
- 전 컴포넌트 색상/타이포/radius/aspect 모두 globals.css `@theme` 토큰 사용.
- `text-base`(Button primary·Pill brand): `--text-base`(font-size)와 이름 충돌 우려 검증 → 컴파일 CSS `.text-base{color:var(--color-base)}`로 확정(의도한 dark 텍스트 색 정상 적용). 문제 없음.
- `PersonAvatar` `rounded-full`(1/1 원형)은 표준 유틸이며 pill 토큰과 시각 동등 — 위반 아님.

### 검증 대상 3개 결정사항
1. **경로 alias `@/src/...`**: tsconfig `"@/*":["./*"]`(루트 기준)와 대조 → `@/src/lib/...`=`./src/lib/...`로 정확히 해소. tsc·build 통과로 실동작 확인. **implementer 주장 정확, PASS.**
2. **Next16 `priority`→`preload`**: `node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md` L293·L1402에서 "Starting with Next.js 16, `priority` deprecated in favor of `preload`" 명시 확인. **주장 사실, PASS.** next.config remotePatterns `/t/p/**`도 URL 빌더와 일치.
3. **framer-motion v12**: `motion.create(Link)`, Button `HTMLMotionProps<"button">` 확장 — tsc 통과. reduced-motion 시 `useReducedMotion()`으로 whileHover/whileTap을 `undefined` 처리 → scale/y 제거, opacity 계열만 잔존(§5). PASS.

### 타입/린트/기타
- `any`·`as` 단언(as const 제외) 없음(grep 확인). motion.ts 튜플 명시 타입. CLAUDE.md 타입 규칙 준수.
- Skeleton shimmer: globals.css `@utility skeleton`가 기존 `shimmer` keyframe + `--animate-shimmer` 토큰 재사용, surface/surface-hover만 사용. 컴파일 CSS 확인. reduced-motion 전역 감쇠 적용. PASS.

### 수정 필요 (FIX)
- `src/components/ui/content-card.tsx:49` — MotionLink className에 `focus-visible:outline-none`이 있어 전역 `:focus-visible` 포커스 링을 제거함. 컴파일 CSS로 특이도 확정: 유틸 셀렉터 `.focus-visible\:outline-none:focus-visible`(0,2,0, utilities 레이어)가 base의 `:focus-visible`(0,1,0)을 이겨 `outline-style:none` 적용 → **카드 링크 키보드 포커스 시 시각적 지시자 전무**. 대체 포커스 스타일도 없음. §6("outline:none만 두고 대체 스타일 없는 경우 금지") 위반이며, ContentCard는 콘텐츠→상세의 주 인터랙션 요소라 영향 큼. 주석("포커스 링은 globals.css :focus-visible 위임")과도 모순.
  - **수정 방향**: `focus-visible:outline-none`을 제거해 전역 2px 포커스 링이 적용되게 한다(현대 브라우저에서 outline은 rounded-lg를 따라감). 링을 카드 라운드에 밀착시키려면 제거 대신 `focus-visible:outline-2 focus-visible:outline-focus focus-visible:outline-offset-2` 명시로 대체 가능하나, 최소 수정은 해당 유틸 제거.

### 종합
빌드/타입/린트/토큰/타입안전성/엣지케이스 모두 통과. 접근성 포커스 링 1건만 부분 수정(FIX) 필요. REDO 아님 — 접근 방식은 정상, 국소 수정으로 해결.
