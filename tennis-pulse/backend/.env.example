# backend/ (Python 크롤러) 전용 — 배치/수동 실행 시 로컬에서 읽힘

# Supabase — posts UPSERT 쓰기 권한 필요. RLS 우회를 위해 anon key가 아닌 service role key를 쓴다
# 사전에 posts 테이블 생성 필요: idxno(bigint, PK) / title(text) / author(text, nullable)
#   / posted_at(date) / views(int) / created_at(timestamptz, default now())
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
