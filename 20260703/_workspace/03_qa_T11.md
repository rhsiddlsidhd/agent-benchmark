## T11 (FR-6 장르 필터 UI + getGenres + discover Route Handler/훅) — 판정: PASS

> QA: qa · 2026-07-06 · 입력: _workspace/02_implementer_T11.md + src/ 실제 코드
> 검증 방식: boundary-qa-checker (경계면 양쪽 동시 읽기 + 교차 비교). tsc/eslint 실제 실행. Tailwind 실제 컴파일로 토큰 해석 검증.

### API↔훅 — 일치

생산자(`src/app/api/discover/route.ts`)와 소비자(`src/features/discover/use-discover-infinite.ts`)를 동시에 대조했다.

- 성공 응답 shape: route 는 `NextResponse.json(data)` 로 `DiscoverResponse = Paginated<Movie> | Paginated<TVShow>`(types.ts)를 반환. 훅 `fetchDiscoverPage<T>` 는 응답을 `Paginated<DiscoverItem<T>>`(`DiscoverItem<"movie">=Movie`, `<"tv">=TVShow`)로 소비. `type` 리터럴 분기가 route(line 65-67)와 훅 제네릭 양쪽에서 동일하게 좁혀져 정확히 매칭된다.
- 래핑 없음: TMDB 원본 `Paginated<T>`(`{ page, results, total_pages, total_results }`, snake_case)를 변환 없이 그대로 패스스루. 훅 `getNextPageParam` 이 `lastPage.page`/`lastPage.total_pages`(snake_case)를 읽어 타입(types.ts:14-19)과 일치. camelCase 변환 지점이 없어 불일치 여지 없음.
- 에러 응답: route 는 `DiscoverErrorResponse = { error: string }`(400/429/5xx/502). 훅은 `!response.ok` 를 `throw` 로 처리(body 파싱 안 함) → shape 계약 어긋남 없음.
- 제네릭 캐스팅으로 컴파일러가 못 잡는 런타임 shape 불일치 없음(discoverByGenre 오버로드가 type 리터럴로 반환 타입을 고정).

### 에러/엣지케이스 — 준수 (01_ARCHITECTURE §4)

- 429/5xx 패스스루: route.ts:70-76. `isTmdbError` 후 `error.status`(400~599)를 그대로 반환, status 0(네트워크/타임아웃)만 502 매핑. 핸들러 자체 재시도 없음(ADR-0004). ✔
- 클라이언트 재시도: 훅 `retry: 1`(line 92)만. 추가 재시도 로직 없음. ✔
- 마지막 페이지 정지: `getNextPageParam` 이 `page < total_pages ? page+1 : undefined`(line 89-90) → 조용히 정지. T5 `useSearchInfinite` 와 동일 패턴. ✔
- 장르 0개 기본 동작: route 가 `with_genres` 생략 + client.ts `sort_by: popularity.desc` → 인기순 디스커버. 훅 `enabled` 기본 true(0개도 유효). 정의됨. ✔
- 잘못된 입력 방어: type 오타 400(line 42-47), genreIds 비정수/음수 제거(line 51-54), page NaN/0/음수→1(line 57-58). ✔
- 예상외 에러 삼키지 않음: `throw error`(line 78)로 Next 기본 500 위임. CLAUDE.md error-handling 준수. ✔
- 결과 없음 `EmptyState` / error.tsx: 화면 조립(T12) 범위. 본 태스크(UI/훅/API 단위)에서는 route 가 `results: []` shape 를 유지하는 것으로 충분. 누락 아님.

### 라우팅 — 일치

- route 파일 경로 `src/app/api/discover/route.ts` → 실제 URL `/api/discover`. 훅 fetch 대상 `/api/discover?${params}`(line 66) 정확히 일치.
- FilterChip/GenreFilter 는 네비게이션 요소가 없음(제어 컴포넌트, onToggle 콜백만). 페이지 `href`/`router.push` 없음 — 선택 상태 URL 동기화는 T12 범위(GenreFilter 주석에 명시).

### 디자인 토큰 — 준수

전 className 을 03_DESIGN §1 토큰 표와 대조. 임의 색상/arbitrary value 없음: `bg-brand`, `bg-surface`, `bg-surface-hover`, `border-border`, `text-content-secondary`, `text-content-primary`, `text-base`, `rounded-pill`, `text-body-sm`, spacing(gap-1.5/2, px-3, py-1.5, pb-1, size-4) 전부 globals.css `@theme` 매핑값.

**selected 칩 전경색 `text-base` 결정 — 타당함(검증 완료).** Tailwind v4 실제 컴파일 결과 `.text-base { color: var(--color-base) }`(#0b0d12)로 해석됨(폰트사이즈 아님 — globals.css 가 `--text-base` 를 정의하지 않아 색상 유틸로 확정). 대비 실측:
- 구현 선택(brand #01b4e4 배경 + text-base #0b0d12 전경): **8.01:1** → NFR-4/§6(4.5:1) 통과.
- 문서 §2.5 문구대로(brand + text-primary #f4f6fb): **2.25:1** → 4.5:1은 물론 3:1도 미달.
- default 칩(surface #141821 + content-secondary #aeb6c6): 8.72:1 → 통과.

즉 문서 §2.5 "selected = brand 배경 + text-primary" 문구를 그대로 따르면 접근성 기준을 위반한다. implementer 는 새 값을 만들지 않고 기존 토큰 `text-base`(=color-base)를 사용해 대비를 확보했고, 이미 QA 통과한 `Pill` brand variant(pill.tsx:21)와 동일한 처리다. 판단 타당. **문서 §2.5의 전경색 표기가 실제 버그**이며, 별도 문서 수정 권고(구현 수정 불필요).

### 그 외 완료 기준

- FilterChip `aria-pressed`(line 47) + 체크 아이콘 병행(색맹 대응, §6 체크리스트). 색상만으로 상태 구분하지 않음. ✔
- 다중 선택: GenreFilter 가 `Set(selectedIds)` 로 판정, id 단위 `onToggle` 제어 컴포넌트. 정확 동작. ✔
- 반응형: `overflow-x-auto md:flex-wrap md:overflow-visible`(genre-filter.tsx:49) — 모바일 가로 스크롤 / 데스크톱 wrap. §2.5 부합. ✔
- 모션: FilterChip `whileTap` + `useReducedMotion` 시 변형 제거(line 42,49). §5 준수. ✔
- 키 미노출: 신규 클라이언트 파일에 `process.env`/`tmdb/client`/토큰 참조 0건(grep 확인). tmdb-client 는 `server-only`, route 만 경유. ✔
- 타입 안전: 신규 5개 파일에 `any`/`as`(as const 포함) 단언 0건. `DiscoverItem<T>` 조건부 타입으로 내로잉. ✔
- queryKey `sortedGenreIds` 정렬(캐시 안정) vs queryFn 원본 `genreIds` 전달 — 원본 불변 유지, TMDB 는 순서 무관이라 정상. 버그 아님. ✔
- getGenres/discoverByGenre 재사용: client.ts 무변경 확인. discoverByGenre 오버로드(line 182-211) 정상. ✔
- `npx tsc --noEmit` EXIT 0 / `npx eslint` EXIT 0 (실제 실행 확인). ✔

### 미검증(범위 외, 누락 아님)

- 화면 end-to-end(선택→그리드→무한스크롤 sentinel), 선택 상태 URL 동기화, EmptyState/ErrorState/Skeleton 노출: **T12(장르 탐색 화면 조립)** 범위. 본 태스크는 UI/훅/API 단위 완성으로 정의됨(planner T11 완료기준 + implementer 기록 line 49 일치).

### 수정 필요

없음. 코드 수정 불필요. (문서 조치 권고 1건: 03_DESIGN §2.5 selected 칩 전경색 표기를 `text-primary`→`text-base`(=color-base)로 정정 — Planner/문서 오너 판단. 구현은 이미 올바름.)
