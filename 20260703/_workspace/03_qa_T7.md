## T7 — 판정: PASS

영화 상세 `/movie/[id]` (FR-3). 신규 5개 파일(page.tsx / person-link.tsx / not-found.tsx / error.tsx / detail.module.css) 전수 검증 + tsc·eslint·build·dev 런타임 확인 완료.

### API↔타입 (SC 직접 호출 경로, Route Handler·훅 없음)
일치. page.tsx 가 소비하는 필드가 client.ts 반환 타입과 정확히 맞물림.
- `getMovie` → `MovieDetail | null`: `release_date`/`runtime: number|null`/`genres: Genre[]`/`tagline`/`overview`/`vote_average`/`backdrop_path`/`poster_path` 모두 types.ts와 일치. `runtime` null·`release_date` 빈문자열 결측 케이스도 타입에 명시돼 있고 대체 문구로 처리.
- `getMovieCredits` → `Credits{cast:CastMember[],crew:CrewMember[]}`: `member.credit_id`/`character`/`profile_path`/`job`/`id`/`name` 사용 필드 전부 존재. snake_case 원본 그대로 소비(변환 없음)로 일관.
- `getMovieRecommendations` → `Paginated<Movie>`: `.results` 언랩 후 `item.id/title/poster_path/release_date/vote_average` 사용 — 일치.
- UI 컴포넌트 prop 계약도 전부 일치: BackdropImage(path·alt·size·sizes·preload), PosterImage(path·alt·size·className), Pill(children·variant), RatingBadge(value·variant), ContentCard(href·title·posterPath·year·rating), PersonAvatar via PersonLink(path·name·role), ErrorState(onRetry).

### 에러/엣지케이스 (§4, §2.9)
전 항목 준수 — 코드 + 런타임 이중 확인.
- notFound 순서: page.tsx:137-145 `getMovie` → `movie===null` 시 `notFound()` → 그 이후에만 `Promise.all([credits, recommendations])`. 없는 id 가 credits throw 로 error.tsx 에 새지 않는 구조. 런타임 확인: `/movie/999999999` → HTTP 404, not-found.tsx("영화를 찾을 수 없습니다")만 렌더, error.tsx("문제가 발생했어요/다시 시도") 마커 0건.
- id 파싱: page.tsx:131 `!Number.isInteger || <=0` → notFound(). 런타임 `/movie/abc` → HTTP 404.
- fetch 실패: error.tsx 세그먼트 Error Boundary 존재, `unstable_retry` 로 재요청. 루트 error.tsx(T4)와 동일 패턴 확인. Next 16.2.10 docs(error.md:117-136 `unstable_retry: () => void`, 재fetch·재렌더) 부합 — IDE 경고는 T4에서 결론난 언어서버 lag false positive.
- 텍스트 결측: 줄거리 "줄거리 정보가 없습니다"(209), 러닝타임 "러닝타임 정보 없음"(152), 연도 "출시일 미정"(151) — 섹션 유지.
- 리스트 결측: cast/keyCrew/recommended 빈 배열 시 섹션 전체 `? ... : null` 숨김(214/235/257).
- 이미지 null: Backdrop/Poster/PersonAvatar 플레이스홀더 위임.

### 라우팅
일치. `src/app/movie/[id]` 실경로 확인. 출연진/제작진 → `/person/${id}`, 추천작 → `/movie/${id}`, not-found → `/`. 런타임 valid 페이지에 /person/ 링크 48건·/movie/ 링크 42건 렌더 확인, 탐색 흐름(PRD §2) 완결.
- 참고(비차단): `/person/[id]` 라우트는 T10 미구현 상태 → 현재 클릭 시 루트 not-found. 03_DESIGN §3.3 이 명시한 스펙대로의 forward-link 이므로 T7 결함 아님. T10 완료 시 자동 연결됨.

### 디자인 토큰
준수. arbitrary value(`[...]`) 없음. 색/타이포/spacing/radius 전부 정의 토큰(text-display, text-h2, text-body, px-gutter(-lg), gap-section, max-page, rounded-lg, shadow-hover, bg-brand, text-brand, from-base 등). detail.module.css 는 `--spacing-card-gap(-lg)`/`--spacing-gutter(-lg)` CSS 변수만 사용, 레일 폭 %는 레이아웃 구조값(home.module.css 동일 규칙). 신규 토큰 생성 없음.

### 타입 안전성 (CLAUDE.md)
`any` 0건, `as` 단언 0건(`as const` 만 REVALIDATE·KEY_CREW_JOBS `readonly`). KeyCrewPerson 등 뷰모델 전용 인터페이스 정의로 느슨한 타입 없음.

### 검증 명령
- `npx tsc --noEmit`: exit 0
- `npx eslint src/app/movie`: exit 0
- `npm run build`: exit 0 — `/movie/[id]` = ƒ(server-rendered on demand), revalidate fetch 레벨 적용
- dev 런타임: valid(27205)=200/full render, invalid(999999999)=404/not-found, 비정수(abc)=404

수정 필요 항목 없음.
